server:
  port: 8083

spring:
  datasource:
    url: jdbc:mysql://localhost:3309/auth_application
    username: root
    password: sayan2005@@S
    driver-class-name: com.mysql.cj.jdbc.Driver

#    hikari:
#      pool-name: HikariCP
#      maximum-pool-size: 5
#      connection-timeout: 30000
#      idle-timeout: 600000
#      max-lifetime: 1800000

  jpa:
    hibernate:
      ddl-auto: update

    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect

    show-sql: true

security:
    jwt:
      secret: ${JWT_SECRET:saiofal_vkasfbehwq_must_be_at_least_64_characters_long_for_hs512_signatures_to_work_properly_12345}
      issuer: ${JWT_ISSUER:api.substring.com}

      access-ttl-seconds: ${JWT_ACCESS_TTL_SECONDS:3600}
      refresh-ttl-seconds: ${JWT_REFRESH_TTL_SECONDS:86400}

      refresh-token-cookie-name: ${JWT_COOKIE_NAME:refreshToken}

      cookie-secure: ${JWT_COOKIE_SECURE:false}
      cookie-http-only: ${JWT_COOKIE_HTTP_ONLY:true}
      cookie-same-site: ${JWT_COOKIE_SAME_SITE:lax}

      cookie-domain: ${JWT_COOKIE_DOMAIN:localhost}